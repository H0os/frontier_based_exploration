name: frontier_exploration
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - pip
  - pip:
      - opencv-python
      - matplotlib
      - torch
      - tf-transformations
