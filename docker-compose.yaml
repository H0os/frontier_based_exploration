# Docker Compose file for TurtleBot Behavior Examples
#
# Usage:
#
# To build the images:
#   docker compose build
#
# To start up a specific service by name:
#   docker compose up <service_name>
#
# To open an interactive shell to a running container:
#   docker exec -it <container_name> bash

services:
  # Base image containing dependencies.
  base:
    image: frontier_exploration:base
    build:
      context: .
      dockerfile: docker/Dockerfile
      args:
        ROS_DISTRO: ${ROS_DISTRO:?}
      target: base
    # Interactive shell
    stdin_open: true
    tty: true
    # Networking and IPC for ROS 2
    network_mode: host
    ipc: host
    # Needed to display graphical applications
    privileged: True
    environment:
      # Needed to define a TurtleBot model type (3 or 4)
      - TURTLEBOT_MODEL=${TURTLEBOT_MODEL:-4}
      # Allows graphical programs in the container.
      - DISPLAY=${DISPLAY}
      - QT_X11_NO_MITSHM=1
      - XAUTHORITY=/root/.Xauthority
      - NVIDIA_DRIVER_CAPABILITIES=all
    volumes:
      # Allows graphical programs in the container.
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - ${XAUTHORITY:-$HOME/.Xauthority}:/root/.Xauthority
    devices:
      - /dev/dri:/dev/dri
    group_add:
      - video

  # Overlay image containing the example source code.
  overlay:
    extends: base
    image: frontier_exploration:overlay
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: overlay

  # Demo simulation world
  demo-world:
    extends: overlay
    command: /bin/bash -lc "source /opt/ros/jazzy/setup.bash && \
                        [ -f /overlay_ws/install/setup.bash ] && source /overlay_ws/install/setup.bash || true && \
                        ros2 launch turtlebot4_gz_bringup turtlebot4_gz.launch.py slam:=true nav2:=true rviz:=true world:=maze"

  # Behavior demo using Python and py_trees
  demo-frontier-detector-grid:
    extends: overlay
    command: ros2 run frontier_exploration map_utils --find_frontiers

  demo-frontier-detector-ml:
    extends: overlay
    command: ros2 run frontier_exploration map_utils --learned_find_frontiers

  demo-explore:
    extends: overlay
    command: ros2 run frontier_exploration exploration 
  
